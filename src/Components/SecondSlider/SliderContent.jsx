
const SliderContent = ({item}) => {
    const {title,subtitle,description,image}=item
  return (
    <div>
        <div>
            <h1></h1>
        </div>
    </div>
  )
}

export default SliderContent